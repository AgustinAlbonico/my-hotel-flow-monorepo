# ========================================
# Backend Environment Variables
# ========================================
#
# ⚠️ NOTA IMPORTANTE: Las variables de entorno están centralizadas en /.env (raíz del monorepo)
#
# Este archivo se mantiene por compatibilidad pero NO ES USADO.
#
# Para configurar variables de entorno:
# 1. Ve a la raíz del proyecto (../../)
# 2. Copia /.env.example a /.env
# 3. Ajusta los valores según tu entorno
#
# Ver /.env.example para la lista completa de variables disponibles
